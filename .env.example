# WebConfig Helper - Environment Variables Template
# Copy this file to .env and update with your configuration

# =============================================================================
# HOST PERMISSIONS
# =============================================================================
# Which websites the extension can access
# Options:
#   - *://*/*                          (all websites)
#   - https://*.example.com/*          (specific domain and subdomains)
#   - https://app.example.com/*        (specific subdomain only)
VITE_HOST_PERMISSION=*://*/*

# =============================================================================
# LOCAL STORAGE KEY
# =============================================================================
# The localStorage key name where your application stores the endpoint URL
# This must match the key your web application uses
VITE_KEY=EndpointUrl

# =============================================================================
# PREDEFINED ENDPOINTS
# =============================================================================
# Configure up to 4 predefined endpoints with labels and URLs
# These will appear as quick-select options in the extension menu

# Endpoint 1 (typically Local development)
VITE_NBT_LABEL=Local
VITE_NBT_URL=http://localhost:3000/

# Endpoint 2 (typically Dev environment)
VITE_FB_LABEL=Dev
VITE_FB_URL=https://dev.api.example.com

# Endpoint 3 (typically Staging/QA environment)
VITE_AUTOTEST_1_LABEL=Staging
VITE_AUTOTEST_1_URL=https://staging.api.example.com

# Endpoint 4 (typically Production environment)
VITE_AUTOTEST_2_LABEL=Prod
VITE_AUTOTEST_2_URL=https://api.example.com

# =============================================================================
# TOKEN CONFIGURATION
# =============================================================================
# Comma-separated list of localStorage keys to search for authentication tokens
# The extension will try these keys first, then search for any key containing "token"
# Common token key names: authToken, accessToken, token, idToken, jwt, bearer
VITE_TOKEN_KEYS=authToken,accessToken,token,idToken,jwt
